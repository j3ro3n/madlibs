import { Component } from '@angular/core';

/*
  Default app component. Don't change this, look at others, instead.

  Template only holds the router module for navigation.
*/
@Component({
  selector: 'app-root',
  templateUrl: './view/base.component.html'
})
export class AppComponent {
  title = 'Madder Libs';
}
